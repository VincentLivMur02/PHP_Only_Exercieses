<?php

/*

Esercizio 2:

- Crea una variabile $somma con valore 0 e una variabile $numero con valore 1. 
- Usando un ciclo while, continua ad aggiungere $numero a $somma finché $somma non supera 50. 
- Stampa il valore finale di $somma.

*/

$sum = 0;
$num = 1;

# need to verify the sum if whether it exceeds 50 or not

while ($num < $sum) { 
    $result = $sum += $num;
    $result++;
    echo $result;
}

// dubbi
